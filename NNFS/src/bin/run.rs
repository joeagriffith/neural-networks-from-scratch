use ndarray::prelude::*;
use std::fs::File;
use std::io::Write;
use std::env::current_dir;

//write hello world out to a txt file
fn main() {
    // let mut file = File::open("hello.txt").expect("Error creating file");
    // file.write_all(b"Hello World!").expect("Error writing to file");
}